'use strict';

/**
 * sleep-cycle-list controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::sleep-cycle-list.sleep-cycle-list');
